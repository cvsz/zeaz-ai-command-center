global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yaml

scrape_configs:
  - job_name: zeaz-ai-command-center
    metrics_path: /api/metrics
    scheme: http
    static_configs:
      - targets:
          - command-center:8765
